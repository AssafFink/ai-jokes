# CLAUDE.md

> אלו ההוראות שלך ל-Claude Code. Claude קורא קובץ זה אוטומטית בתחילת כל שיחה בפרויקט. שמור אותו קצר וברור.

## על הפרויקט
- **מה אני בונה:** אתר דף-יחיד שמייצר בדיחה באמצעות AI (ChatGPT) לפי נושא שהמשתמש מזין, או בנושא רנדומלי אם התיבה ריקה. Client ב-React/TypeScript (Vite), Server ב-Node/TypeScript (Express) עם Route יחיד, ללא מסד נתונים. אירוח כשירות יחיד ב-Render.

## היכן נמצאים מסמכי ה-spec
כל מסמכי התכנון נמצאים בתיקיית `spec/`:
- `spec/PRD.md` — מה האפליקציה עושה ולמה
- `spec/ARCHITECTURE.md` — ה-tech stack, הנתונים, והשירותים
- `spec/DESIGN.md` — המסכים והעיצוב הוויזואלי
- `spec/MILESTONES.md` — תוכנית הבנייה (Milestones ו-Slices)
- `spec/plans/` — קובץ תוכנית מפורט לכל Milestone (milestone-1.md, ...), נכתב לפני בנייתו

יש לקרוא את ה-spec (PRD, ARCHITECTURE, DESIGN, MILESTONES) לפני הבנייה. אין צורך לקרוא קובצי plan ישנים — הקוד הוא מקור האמת; רלוונטי רק ה-plan של ה-Milestone הנוכחי.

## איך אנחנו עובדים יחד
- לבנות **Milestone אחד בכל פעם**, לפי `spec/MILESTONES.md`. לא לבנות הכל בבת אחת.
- אחרי כל Milestone, **לעצור** כדי שאבדוק שהוא עובד לפני שממשיכים הלאה.
- לעדכן את `spec/MILESTONES.md`: לסמן משימות שהושלמו תוך כדי העבודה.
- **לא לבצע commit ל-Git ולא deploy בלי לשאול אותי קודם.**
- כשצריך חשבונות, סיסמאות או מפתחות API — **לעצור ולומר לי**. אני אפתח את החשבונות ואדביק את המפתחות בעצמי.

## קונבנציות קוד
- כל התיעוד וההערות (comments) בקוד — **באנגלית בלבד**, בכל שפת פיתוח ובכל פרויקט, גם אם מסמכי הפרויקט בעברית.

## טיפול בסתירות בין מסמכים
- **מי מנצח (לפי תחום):** `PRD.md` מנצח בהחלטות מוצר ופיצ'רים. `DESIGN.md` מנצח בענייני UI ומסכים. `ARCHITECTURE.md` מנצח בענייני טכנולוגיה ונתונים. יש להשתמש בכלל זה כדי ליישב סתירה שנופלת בבירור בתוך תחום אחד.
- **כשעדיין לא ברור:** אם נשארת סתירה אמיתית שהכלל אינו פותר — **לעצור ולשאול אותי לפני שמחליטים**. לא ליישב אותה לבד.
- **כפילות זהה זה בסדר** — פשוט להמשיך.

## פקודות אבן דרך (טריגרים קצרים)
"אבן דרך" = Milestone. "אבן הדרך הנוכחית" = אבן הדרך הראשונה שעדיין לא הושלמה ב-MILESTONES.md (אין צורך לציין מספר). כל טריגר מקבל כמה ניסוחים:

- **תכנון** — "תכנן את אבן הדרך הבאה" / "תכנן את ה-Milestone הבא": קרא את ה-spec והקוד הרלוונטי, הכן תוכנית מפורטת לאבן הדרך הנוכחית בלבד, ושמור אותה ל-spec/plans/milestone-<N>.md. אל תכתוב קוד.
- **בנייה** — "בנה את אבן הדרך הבאה" / "בנה את ה-Milestone הבא": בנה את אבן הדרך הנוכחית לפי spec/plans/milestone-<N>.md, עדכן את MILESTONES.md, והרץ מקומית כדי שאבדוק. אל תבצע commit.
- **שמירה** — "שמור שינויים בגיטהאב" / "שמור שינויים ב-GitHub": git add + commit (הודעה ברורה) + push.

### פורמט תגובה לפקודות אלו
- עבוד בשקט, בלי הערות באמצע העבודה.
- בסיום תן סיכום קצר של 2–3 שורות בלבד (מה נעשה + מה הלאה).
- חריג: אם יש שגיאה או שאתה צריך החלטה/מידע — עצור מיד ושאל.

## פקודות
> למלא לאחר שהפרויקט מוקם (Claude יכול לייצר אותן).
- **הרצה (dev):** `<command>`
- **בדיקות (test):** `<command>`
